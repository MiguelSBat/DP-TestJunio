- A query that returns the ratio of newspapers with at least one CosaX.


	select count(distinct c.newspaper)*100/(select count(n) from Newspaper n) from CosaX c);


- A query that returns the administrator/s who has written more CosaXs.


	select count(c) from CosaX c group by c.administrator order by count(c) DESC;

	select c.administrator from CosaX c group by c.administrator having count(c)=*primer resultado de la otra query*;

	*Debido a como hemos diseñado el sistema para optimizar el rendimiento, es imposible con la tecnologia con la que 
	trabajamos realizar esta peticion en una sola query, por lo tanto se deberia de dividir en 2 y controlarlo por servicios.
